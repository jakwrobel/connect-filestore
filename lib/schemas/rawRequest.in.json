{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "required": false,
      "title": "Endpoint URL (will be added to the previously set Resource server URL)"
    },
    "requestType": {
      "type": "string",
      "required": true,
      "title": "Request type"
    },
    "customHeaders": {
      "type": "string",
      "required": true,
      "title": "Headers"
    },
    "requestBody": {
      "type": "string",
      "required": false,
      "title": "Request's body"
    },
    "dataToSend": {
      "type": "string",
      "required": false,
      "title": "Data to send (will be encoded and added to the request's body)"
    }
  }
}
