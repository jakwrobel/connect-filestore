stages:
  - install
  - test

default:
    image:
        name: docker-hub.squiz.net/docker/base-images/node:22-alpine

install-dependencies:
  stage: install
  script:
    - npm install

run-tests:
  stage: test
  script:
    - npm test